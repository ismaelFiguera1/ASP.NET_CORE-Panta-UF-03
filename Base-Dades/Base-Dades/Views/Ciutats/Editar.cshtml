<section>
	<form asp-action="UpdateCiutatFINAL" method="post">
		<table>
			<tr>
				<th>ID</th>
				<th>Nom</th>
				<th>Codi-Pais</th>
				<th>Districte</th>
				<th>Poblacio</th>
			</tr>
			<tr>
				<td>@Model.ciutat.id</td>
				<td><input type="text" name="name" required value="@Model.ciutat.name" /></td>
				<td>
					<select name="countrycode">
						<option value="@Model.ciutat.countrycode">@Model.pais.Name</option>
						@foreach(Pais pais in Model.llistaPaisos){
							<option value="@pais.Code">@pais.Name</option>
						}
						
					</select>
				</td>
				<td><input type="text" name="district" required value="@Model.ciutat.district" /></td>
				<td><input type="number" name="population" required value="@Model.ciutat.population" /></td>
			</tr>
		</table>
		<input type="hidden" name="id" value="@Model.ciutat.id" />
		<button type="submit">UPDATE</button>
	</form>

	@if (TempData["Missatge"] != null)
	{
		<div style="background-color: #d4edda; padding: 10px; border-radius: 5px; color: #155724;">
			@TempData["Missatge"]
		</div>
	}

	
</section>